package cn.com.sky.patterns.behavioral.command;

/**
 * <pre>
 *
 * 应用场景：
 *   Command:
 * 把一个或一些命令封装到一个对象中。
 * java.lang.Runnable
 * javax.swing.Action
 *
 * Tomcat 中的命令模式:
 * Connector 作为 抽象请求者，
 * HttpConnector 作为 具体请求者。
 * HttpProcessor 作为 命令。
 * Container 作为 命令的抽象接受者，
 * ContainerBase 作为 具体的接受者。
 * 应用服务器 Server组件是 客户端。
 *
 * </pre>
 */
public class usage {

}